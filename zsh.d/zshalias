###
# basic
###
alias v='nvim'
alias vi='nvim'
alias vim='nvim'
alias vimdiff='nvim -d'
alias rm='rm -i'
alias ..='cd ..'
alias mv='mv -i'
alias cp='cp -i'
alias c='clear'
alias relogin='exec $SHELL -l'
alias path="echo $PATH | tr ':' '\n'"
if [ -e "$HOME/.zhistory" ]; then
    alias shell-history='cat $HOME/.zhistory | cut -b 16- | head -n 5000'
fi

###
# Git
###
alias g='lazygit'
# add
alias ga='git add'
alias gau='git add -u'
# branch
alias gb='git branch'
alias gbD='git branch -D'
alias gba='git branch --all'
alias gbd='git branch --delete'
# checkout
alias gcho='git checkout'
# commit
alias gc='git commit'
alias gcm='git commit -m'
alias gcam='git commit --amend'
# config
alias gc-list='git config -l'
# diff
alias gd='git diff'
alias gds='git diff --staged'
# fetch
alias gf='git fetch'
alias gfp='git fetch --prune'
# grep
alias gg='git grep'
# log
alias glg='git log --graph --all --decorate'
alias glo='git log --oneline -n 30 --format="%C(green)%C(bold)%cd %C(auto)%h%d %s (%an)" --color=always --date=format:"%Y/%m/%d %H:%M:%S"'
# ls-files
alias gcheck-ignored='git ls-files -io --exclude-standard'
# merge
alias gm='git merge'
alias gms='git merge --squash'
# push
alias gpoH='git push origin HEAD'
# status
alias gs='git status --short'
# tag
alias gt='git tag'
alias gta='git tag -a'
alias gtd='git tag -d'
# submodule
alias gss='git submodule status'
alias gsa='git submodule add'
alias gsu='git submodule update'
alias gsur='git submodule update --recursive'
# worktree
alias gwa='git worktree add'
alias gwl='git worktree list'
alias gwp='git worktree prune'

# composite command
alias gcommit-only-deleted='git ls-files --deleted | xargs git commit'

function _mr_checkouted_branch() {
    local branch=$($DOTFILES_DIR/scripts/mru-branch-list.sh | fzf)
    if [ -z "$branch" ] ; then
        echo 'canceled swiching branch'
        return 1
    fi
    git checkout "$branch"
}
alias gcho-recent-branch='_mr_checkouted_branch'

###
# Docker
###
alias docker-restart='docker-compose down && docker-compose build && docker-compose up -d'

###
# ripgrep
###

alias rgf='rg --files-with-matches'

###
# find
###

alias fdhere='find . -name'

###
# tmux
###
alias ta='tmux a'
alias tks='tmux kill-server'
alias tls='tmux ls'
alias tns='tmux new -s'
alias t='tmux-session-fzf.sh'

###
# 競技プログラミング用(Go)
###
alias gr='go run'

###
# navi
###
alias n='navi'

###
# fzwiki
###
alias wikija='fzwiki -o'

###
# translate-shell
###
alias enja='trans en:ja'
alias jaen='trans ja:en'

###
# ubuntu
###
alias pbcopy='xsel --clipboard --input'
alias pbpaste='xsel --clipboard --output'
alias tmux-copy='tmux save-buffer - | pbcopy'
